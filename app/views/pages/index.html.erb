<h1>Pages</h1>
<p>
  <%= link_to 'Create New Page', new_page_path %>
</p>
<% @pages.each do |page| %>
  <h2><%= page.title %></h2>
  <%= link_to 'show!', page_path(page) %>
  <%= link_to 'edit!', edit_page_path(page) %>

<% end %>